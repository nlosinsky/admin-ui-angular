import { Routes } from '@angular/router';
import { TransactionsTableComponent } from '@views/transactions/transactions-table.component';

export const ROUTES: Routes = [
  {
    path: '',
    component: TransactionsTableComponent
  }
];
